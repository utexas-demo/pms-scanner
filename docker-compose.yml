services:
  pms-scanner:
    build: .
    restart: unless-stopped
    volumes:
      - /Volumes/aria/ARIAscans:/data/incoming   # ARIAscans network share → container watch dir
    env_file:
      - .env
